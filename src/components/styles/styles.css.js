import styled from "styled-components";

const styles = {
    InitialText: styled.div`
        font-size: 30px;
        position: absolute;
        top: 50%;
        right: 50%;
        transform: translate(50%, -50%);
    `,
}

export default styles;